# Список нечётных чисел.
# Программа генерирует список из нечётных чисел в диапазоне от А до B. 

A = int(input('Введите первое число: '))
B = int(input('Введите второе число: '))

lst = [x for x in range(A, B + 1) if x % 2 != 0]

print('Список нечётных чисел от', A, 'до', B, ':', lst)

